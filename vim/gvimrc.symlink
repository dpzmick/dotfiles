if has("gui_macvim")
    " Make macvim transparent
    " set transparency=15
    " hide toolbar and scroll bar
    set guioptions-=r
    set guioptions-=L
endif
if has("gui_running")
    set guioptions-=m
    set guioptions-=T
    set guioptions-=r
    set guioptions-=L
endif
