if has("gui_macvim")
    " Make macvim transparent
    " set transparency=15
    " hide toolbar and scroll bar
    set guioptions-=r
    set guioptions-=L
    " set the font
    set guifont=Meslo\ LG\ M\ for\ Powerline:h11
endif
if has("gui_running")
    set guioptions-=m
    set guioptions-=T
    set guioptions-=r
    set guioptions-=L
endif
